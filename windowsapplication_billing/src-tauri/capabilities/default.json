{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Taj POS",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "store:default",
    "updater:default",
    "notification:default",
    "sql:default",
    "fs:default",
    "dialog:default"
  ]
}
